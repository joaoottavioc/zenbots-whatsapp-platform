# --- Frameworks Principais ---
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlmodel==0.0.19
pydantic[email]==2.7.1
python-dotenv==1.0.1

# --- Banco de Dados ---
asyncpg==0.29.0
pgvector==0.2.5

# --- IA e Machine Learning (Otimizado) ---
# Força a instalação da versão leve (CPU) do PyTorch ANTES do sentence-transformers
# Isso evita o download dos pacotes gigantes da NVIDIA durante o build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers==3.0.1
# protobuf is required by transformers' slow-tokenizer fallback (used by
# the multilingual MiniLM tokenizer). Without it, SentenceTransformer init
# crashes with ImportError → embedding model never loads → semantic search
# and semantic router silently degrade to keyword/ILIKE only. Discovered
# 2026-04-09 when Pão do Jão's bot tests scored 14% comprehension because
# every embedding-dependent path was crashing.
protobuf>=4.25
openai==1.35.3
python-multipart
pymupdf
pdfplumber
Pillow

# --- Autenticação e Utilitários ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
httpx==0.27.0
google-auth==2.35.0

mercadopago
pytz
redis
arq
python-json-logger==2.0.7
fastapi-mail==1.4.1
boto3
alembic

pytest==8.2.2
pytest-asyncio==0.23.8
pytest-mock==3.14.0